To run/debug:
Click “Add Configuration…” in the top-right dropdown (where the run button should be).

Click the + button on the top-left of the dialog.

Select “Android App”.

Give it a name (like App).

In the “Module” dropdown, select your app module (usually called app).

Click Apply → OK.